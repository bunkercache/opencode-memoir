name: opencode

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

# Disable local Memoir plugin in CI to avoid conflicts
env:
  BUNKERCACHE_MEMOIR_DISABLED: '1'

jobs:
  opencode:
    if: |
      github.repository == 'bunkercache/opencode-memoir' &&
      (
        contains(github.event.comment.body, ' /oc') ||
        startsWith(github.event.comment.body, '/oc') ||
        contains(github.event.comment.body, ' /opencode') ||
        startsWith(github.event.comment.body, '/opencode')
      )
    runs-on: blacksmith-4vcpu-ubuntu-2404
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: ./.github/actions/checkout-v6-compat
      - uses: ./.github/actions/setup-bun

      - name: Run opencode
        uses: sst/opencode/github@3331b0600aa6f2fae65609ffe870ca155fba7b25 # v1.0.222
        env:
          OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
          OPENCODE_PERMISSION: '{"bash": "deny"}'
        with:
          model: opencode/claude-opus-4-5
